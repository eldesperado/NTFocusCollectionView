use_frameworks!

source 'https://github.com/CocoaPods/Specs.git'

platform :ios, '8.0'
# Inhibits all the warnings from the CocoaPods libraries.
inhibit_all_warnings!

# Project pods
target 'NTFocusCollectionView' do
	pod 'Reveal-iOS-SDK', '~> 1.5.1', :configurations => ['Debug']
end